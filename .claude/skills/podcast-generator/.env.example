# Copier ce fichier vers .env (gitignoré) et remplir la valeur.
# OPENROUTER_API_KEY donne accès à Voxtral (TTS), OpenAI shimmer (TTS backup),
# et Gemini 3.1 Flash Image Preview (cover) via une seule clé OpenRouter.
OPENROUTER_API_KEY=sk-or-v1-...
